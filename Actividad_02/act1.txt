double potencia (double x, int y) {
    double t;
    if (y == 0)
        return 1.0;
    if (y % 2 == 1)
        return x * potencia(x, y - 1);
    else {
        t = potencia(x, y / 2);
        return t * t;
    }
}
